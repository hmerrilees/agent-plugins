{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v jj >/dev/null 2>&1 || { echo 'active-descriptions: jj (Jujutsu) is required but not found. Install: https://jj-vcs.github.io/jj/latest/install-and-setup/' >&2; exit 1; } && command -v cargo >/dev/null 2>&1 || { echo 'active-descriptions: cargo (Rust toolchain) is required but not found. Install: https://rustup.rs/' >&2; exit 1; }",
            "timeout": 5
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "cargo build -r --manifest-path ${CLAUDE_PLUGIN_ROOT}/Cargo.toml 2>/dev/null",
            "timeout": 300,
            "async": true
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/require-jj.sh",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "msg=$(cargo run -r --manifest-path ${CLAUDE_PLUGIN_ROOT}/Cargo.toml -- 'trunk()..@ ~ empty()' 2>/dev/null) && [ -n \"$msg\" ] && echo \"$msg\" >&2 && exit 2",
            "timeout": 300
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "msg=$(cargo run -r --manifest-path ${CLAUDE_PLUGIN_ROOT}/Cargo.toml -- 'default@..@ ~ empty()' 2>/dev/null); rc=$?; if [ -n \"$msg\" ]; then echo \"$msg\" >&2; exit 2; elif [ \"$rc\" -ne 0 ]; then printf '{\"hookSpecificOutput\":{\"additionalContext\":\"active-descriptions: SubagentStop staleness check skipped — could not resolve default@. The hook requires the main workspace to be named default.\"}}'; fi",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
